import { makeStyles } from '@material-ui/core'

const useStyles = makeStyles({
  progress: {
    width: '100%',
    padding: '1.5rem 0',
  },
  boxContainer: {
    display: 'flex',
    alignItems: 'center',
  },
  boxProgress: {
    width: '100%',
  },
})
export default useStyles
